
server:
  port: 7001

#actutor enable
management:
  endpoints:
    web:
      exposure:
        include: health,info

  #info enabled
  info:
    env:
      enabled: true
    java:
      enabled: true

info:
  app: ${spring.application.name}
